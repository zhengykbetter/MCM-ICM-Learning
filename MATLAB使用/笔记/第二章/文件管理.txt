在命令行输入脚本名称可以运行该脚本
首先在当前文件夹中搜索文件，然后在搜索路径中寻找，直到找到文件为止，若还是没有找到则报错
可以在matlab装机文件夹——tool中找到很多函数，其中未加密的即可打开查看，对将来使用参考价值很大
（m结尾公开，p结尾加密）
新建函数也是一种脚本，可以实现某种功能，有默认也可以自己编写
matlab会现在工作区中搜索变量，再在文件路径中寻找
cd加文件夹路径可以改变工作文件夹，要么只用字母，要么cd('***')的方法
自己的文件名称如果和matlab自带命令相同并且加入搜索路径，可能导致命令执行的未知错误

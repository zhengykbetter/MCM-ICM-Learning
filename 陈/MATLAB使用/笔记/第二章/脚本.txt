ctrl+n或者主页中新建脚本，默认位置是工作文件夹下面
MATLAB文件夹不能使用中文命名（idea也是，但在vscode的测试中可能有.txt以外的文件能用中文命名，大型记事本实锤了）
除了编译器中的运行，可以通过鼠标选中代码并选择执行以执行内容
如果运行代码时出现“未定义函数或变量code1”是因为在当前工作文件夹下没有找到该code1.m文件
在编辑器打开的情况下“视图”中高亮显示当前行，行号，数据提示非常有用，创建好后可以额外打开
